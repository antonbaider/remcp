{
  "name": "remcp",
  "version": "0.2.72",
  "private": true,
  "description": "ReMCP: secure remote MCP bridge. Device client and first-party local runtime.",
  "type": "module",
  "license": "MIT",
  "homepage": "https://remcp.site",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "check": "node scripts/check-plugin.mjs && node scripts/check-claude-plugin.mjs && node scripts/check-distribution.mjs && npm run check --workspaces --if-present",
    "claude:check": "node scripts/check-claude-plugin.mjs",
    "distribution:check": "node scripts/check-distribution.mjs",
    "eval:tool-selection": "node scripts/score-tool-selection-eval.mjs",
    "test": "npm run test --workspaces --if-present"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/antonbaider/remcp.git"
  }
}
