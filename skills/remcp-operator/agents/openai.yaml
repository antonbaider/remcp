interface:
  display_name: "ReMCP operator"
  short_description: "Operate a paired computer safely, including screenshot-based visual verification"
  default_prompt: "Inspect my project on the paired laptop and tell me what changed."
dependencies:
  tools:
    - type: "mcp"
      value: "remcp"
      description: "Tools for computers explicitly paired with ReMCP"
      transport: "streamable_http"
      url: "https://remcp.site/mcp"
