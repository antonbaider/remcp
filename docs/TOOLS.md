# ReMCP tool reference

> Generated by `scripts/generate-web-tool-docs.mjs` from the production MCP catalog. Do not edit this file by hand.

The production catalog contains **93 tools** when custom widgets are enabled: **83 device tools**, **7 account/fleet tools**, and **3 chat-presentation tools**. Setting `REMCP_CUSTOM_WIDGETS_ENABLED=false` switches the server to the 90-tool native-only surface without render tools or preview references. Hosted discovery stays on this stable release catalog so clients that cache installation-time schemas do not lose newer tools when only a smaller-capability computer is online. Each connected computer still advertises its exact live subset through `list_devices`, and the server checks that selected device immediately before dispatch.

The three presentation tools render exact short-lived file/image/terminal previews that were already returned by source tools. They never repeat the file read, screenshot, or process launch just to show UI.

For visible interaction, use **Accessibility/UI Automation → browser DOM/CDP → OCR → coordinates**. Prefer structured document tools over automating Office applications when the task is about file content rather than interactive formatting.

Required parameters are marked with `*`. Tool descriptions, parameter names/types, and safety hints below come from the same catalog that the server advertises.

## Account & fleet

Find your machines, inspect account state, create pairing commands, and review activity.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `list_devices` | read-only | None | Use this when the target computer is not already unambiguous. Call it first to list computers paired to the signed-in ReMCP account and see which ones are online. |
| `device_action` | changes state | `device*` (string), `action*` ("ping" \| "rename" \| "enable" \| "disable" \| "remove"), `name` (string) | Use this when the user needs account-level control of one paired computer: ping it, rename its account-visible label, temporarily enable/disable it, or permanently remove (revoke) it. Use list_devices first when the target is ambiguous. Do not use this for local runtime preferences such as telemetry/read limits; use set_config_value on the computer for those. |
| `who_am_i` | read-only | None | Use this when the person asks which ReMCP account they are signed in as, how many machines are paired and online, or how many tool calls they have made lately. |
| `create_pairing_command` | local state | None | Use this when the person wants to connect another computer to their ReMCP account, or asks how to add a device: returns the exact command to run on that machine, the pairing code, and when it expires. The command installs ReMCP as a background service and connects the machine; the same command is returned while its code is still valid. |
| `get_recent_tool_calls` | read-only | `limit` (number), `device` (string), `tool` (string) | Use this when you need to recover recent ReMCP activity or answer what was just done. It returns the most recent tool calls with the computer they ran on, success state and duration. |
| `get_usage_statistics` | read-only | `days` (number), `device` (string), `tool` (string) | Use this when the person asks for account-level historical ReMCP usage over days: call counts, success rate, average duration, and per-tool/per-device breakdowns. Use get_runtime_stats instead for counters from the current local runtime session on one machine. |
| `get_configuration` | read-only | None | Use this when the person asks how the hosted ReMCP deployment/account is configured: server version and endpoint, runtime release installed on devices, retention/telemetry policy, hosted limits, and paired-computer versions. Use get_runtime_info for one computer's local runtime roots, policy and limits; use get_system_info for that computer's OS/CPU/memory/disk state. |

## Chat presentation

Render already-returned file, image, and terminal data inside MCP Apps-capable chats without repeating the source action.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `render_file_preview` | read-only | `preview_id*` (string) | Use this when the file preview app needs to recover from late or missing result metadata. Pass the source read_file structuredContent.preview.id. This presentation-only app helper rehydrates the exact already-returned file and never re-reads the computer. |
| `render_image_preview` | read-only | `preview_id*` (string) | Use this when the image preview app needs to recover a read_image or screenshot result from late or missing result metadata. Pass the source tool structuredContent.preview.id. This presentation-only app helper rehydrates the exact validated image bytes already returned and never captures or reads the computer again. |
| `render_terminal_preview` | read-only | `preview_id*` (string) | Use this when the terminal preview app needs to recover from late or missing result metadata. Pass the source process tool structuredContent.preview.id. This presentation-only app helper rehydrates the already-returned output; refresh reads the same process and never reruns the command. |

## Read & inspect files

Return model-readable text, structured data, and native MCP image content. Also inspect binary data, metadata, hashes, directories, and diffs.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `read_file` | read-only | `path*` (string), `offset` (number), `length` (number), `device*` (string) | Use this when the task calls for plain text files on a paired computer: source code, configuration, Markdown, logs, scripts, or other line-oriented text. Use read_document instead for PDF, DOCX, XLSX and other structured document content. read_file keeps legacy PDF/DOCX extraction for compatibility but should not be the model’s first choice for those formats. |
| `read_files` | read-only | `path` (string), `pattern` (string), `max_files` (number), `max_lines_per_file` (number), `include_ignored` (boolean), `device*` (string) | Use this when the exact file list is not already known and the user wants the contents of every file matching one glob under a directory. Use start_search instead to find filenames or text matches without reading every matched file. If exact paths are already known, use read_multiple_files; for one known path use read_file. |
| `read_multiple_files` | read-only | `paths*` (array<string>), `device*` (string) | Use this when the user names two or more exact file paths to read in one call. Do not use it for glob discovery: use read_files instead. For one known path use read_file. |
| `read_image` | read-only | `path*` (string), `device*` (string) | Use this when the user wants to inspect or show an image that lives on a paired computer, such as a screenshot, photo, or diagram. It returns native MCP image content; do not use read_binary merely to display an image. |
| `read_binary` | read-only | `path*` (string), `offset_bytes` (number), `length_bytes` (number), `device*` (string) | Use this when the user needs a non-text file transferred or inspected byte for byte as base64 chunks. For a normal image that should be shown or visually inspected, use read_image instead. |
| `hash_file` | read-only | `path*` (string), `algorithm` ("sha256" \| "sha1" \| "md5"), `device*` (string) | Use this when the user wants a checksum to verify a download, compare two files, or confirm a transfer. |
| `list_directory` | read-only | `path*` (string), `depth` (number), `pattern` (string), `device*` (string) | Use this when the user wants to see what a directory on a paired computer contains, optionally filtered by a glob. |
| `get_file_info` | read-only | `path*` (string), `device*` (string) | Use this when the user needs the size, type, permissions, or modification time of one file or directory before deciding what to do with it. |
| `diff_files` | read-only | `left*` (string), `right*` (string), `context_lines` (number), `device*` (string) | Use this when the user wants to see how two text files differ, before or after a change. |

## Write & edit files

Create, edit, move, copy, archive, permission, and delete files or directories on the selected machine.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `write_file` | changes state | `path*` (string), `content*` (string), `mode` ("rewrite" \| "append"), `device*` (string) | Use this when the user asks to create or replace the complete contents of exactly one text file. For a small exact-block edit use edit_block, for a known line range use replace_lines, and when you already have a unified diff use apply_patch. For two or more independent complete files use write_files; use write_binary for binary bytes. |
| `write_files` | changes state | `files*` (array<object>), `device*` (string) | Use this when the user asks to create or replace the complete contents of two or more independent text files in one batch. For one path use write_file; for partial edits use edit_block, replace_lines, replace_in_files, or apply_patch instead of rewriting whole files. |
| `write_binary` | changes state | `path*` (string), `data*` (string), `mode` ("rewrite" \| "append"), `device*` (string) | Use this when the user needs base64 data written to a file on a paired computer byte for byte, including files too large for a single call. |
| `apply_patch` | changes state | `patch*` (string), `path` (string), `dry_run` (boolean), `device*` (string) | Use this when you already have a unified diff with one or more hunks and want to apply it to one or several files. Do not use write_file for the same patch-shaped change, and prefer edit_block when the change is one exact text block rather than a diff. |
| `set_permissions` | changes state | `path*` (string), `mode*` (string), `recursive` (boolean), `uid` (number), `gid` (number), `device*` (string) | Use this when the user asks to change mode bits or ownership of a file or directory, for example to make a script executable. |
| `edit_block` | changes state | `file_path*` (string), `old_string*` (string), `new_string*` (string), `expected_replacements` (number), `allow_fuzzy` (boolean), `dry_run` (boolean), `device*` (string) | Use this when one known text block in one file should be replaced, with a whitespace-tolerant fallback and optional dry-run diff. Use write_file only when replacing the whole file, and replace_lines when the intended target is a known line range rather than matching text. |
| `replace_lines` | changes state | `path*` (string), `start_line*` (number), `end_line*` (number), `content*` (string), `dry_run` (boolean), `device*` (string) | Use this when the intended edit target is a specific 1-based line range in one file, optionally as a dry run. Use edit_block for matching an exact text block and write_file for replacing the complete file. |
| `replace_in_files` | changes state | `path*` (string), `pattern*` (string), `replacement*` (string), `filePattern` (string), `regex` (boolean), `dry_run` (boolean), `maxFiles` (number), `device*` (string) | Use this when the same text or regular-expression replacement should be searched and applied across multiple files under a path, such as a rename or version bump. For one known block use edit_block; for one complete known file use write_file. |
| `move_to_trash` | changes state | `source*` (string), `device*` (string) | Use this when the user wants a file or directory removed but still recoverable from the system trash. |
| `create_directory` | local state | `path` (string), `paths` (array<string>), `device*` (string) | Use this when the user asks to create a directory, including any missing parent directories. |
| `move_file` | changes state | `source*` (string), `destination*` (string), `overwrite` (boolean), `device*` (string) | Use this when the user asks to move or rename exactly one file or directory from one source to one destination. For two or more moves use move_paths. |
| `copy_file` | changes state | `source*` (string), `destination*` (string), `overwrite` (boolean), `device*` (string) | Use this when the user asks to copy exactly one regular file to a new path on the same computer. For directories or two or more copies use copy_paths. |
| `copy_paths` | changes state | `paths*` (array<object>), `overwrite` (boolean), `device*` (string) | Use this when the user asks to copy a directory recursively or two or more paths in one request. For exactly one regular file use copy_file. |
| `move_paths` | changes state | `paths*` (array<object>), `overwrite` (boolean), `device*` (string) | Use this when the user asks to move or rename two or more files/directories in one batch. For one source/destination pair use move_file. |
| `delete_path` | changes state | `path*` (string), `recursive` (boolean), `device*` (string) | Use this when the user explicitly asks to remove exactly one file or directory and does not need it recoverable. Use delete_paths for two or more targets, or move_to_trash when reversibility matters. |
| `delete_paths` | changes state | `paths*` (array<string>), `recursive` (boolean), `device*` (string) | Use this when the user explicitly asks to remove two or more named files or directories in one batch. For one target use delete_path; use move_to_trash when reversibility matters. |
| `create_archive` | changes state | `paths*` (array<string>), `destination*` (string), `format` ("tar" \| "tar.gz" \| "zip"), `device*` (string) | Use this when the user wants files or a directory packed into a tar, tar.gz, or zip archive, for example before a transfer or a backup. |
| `extract_archive` | changes state | `archive*` (string), `destination` (string), `device*` (string) | Use this when the user wants an existing tar or zip archive unpacked on a paired computer. |

## Screenshots & search

Capture the desktop and run bounded file-name or content searches with explicit search sessions.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `take_screenshot` | local state | `directory` (string), `keep` (boolean), `device*` (string) | Use this when the user needs pixels for the entire desktop. Prefer ui_snapshot or browser_snapshot for semantic inspection, and screenshot_region for one window, monitor, or rectangle. |
| `start_search` | local state | `path*` (string), `pattern*` (string), `searchType` ("content" \| "files"), `filePattern` (string), `ignoreCase` (boolean), `maxResults` (number), `includeHidden` (boolean), `includeIgnored` (boolean), `contextLines` (number), `literalSearch` (boolean), `device*` (string) | Use this when the user wants to find filenames or perform a content search under a path and does not need every matching file read in full. Use read_files instead when a glob is already known and the contents of every matched file are the desired result. Starts a search session and returns the first matches. |
| `get_more_search_results` | read-only | `sessionId*` (string), `offset` (number), `length` (number), `device*` (string) | Use this when a search started with start_search still has more matches to return. |
| `stop_search` | local state | `sessionId*` (string), `device*` (string) | Use this when a search session is no longer needed and should release its resources. |
| `list_searches` | read-only | `device*` (string) | Use this when you need to know which search sessions are still active on a machine. |

## Computer state & windows

Take one semantic computer snapshot, discover windows and displays, launch apps, manage windows, and capture only the pixels that matter.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `computer_snapshot` | read-only | `max_ui_nodes` (number), `max_ui_depth` (number), `include_ui` (boolean), `ui_scope` ("active" \| "desktop"), `semantic_tree` (boolean), `ui_browser_dom` (boolean), `include_screenshot` (boolean), `screenshot_region` (array<number>), `screenshot_window_id` (string), `screenshot_monitor` (string), `screenshot_monitor_index` (number), `screenshot_padding` (number), `grid_columns` (number), `grid_rows` (number), `include_browser` (boolean), `browser_max_nodes` (number), `include_ocr` (boolean), `ocr_language` (string), `ocr_max_words` (number), `ocr_psm` (number), `ocr_timeout_ms` (number), `device*` (string) | Use this when you need the first compact view of an unfamiliar desktop state, including windows, semantic UI, displays, clipboard metadata and optional pixels/browser/OCR. Prefer it over repeated screenshots; narrow later with ui_find, browser_find or screenshot_region. |
| `list_windows` | read-only | `include_monitor` (boolean), `device*` (string) | Use this when the user needs to discover top-level desktop windows or obtain a stable window id/PID/bounds before window_action, screenshot_region or targeted input. Use ui_snapshot instead when you need controls inside a window; prefer list_windows over screenshot-based window guessing. |
| `display_inventory` | read-only | `device*` (string) | Use this when monitor-specific screenshots, cross-monitor geometry or coordinate work require monitor bounds, scale and primary-display information. |
| `launch_app` | open-world | `app` (string), `path` (string), `args` (array<string>), `cwd` (string), `wait_for_window` (string), `wait_timeout_ms` (number), `device*` (string) | Use this when the user needs to start a desktop application by executable/path or friendly application name without a shell. Prefer it over start_process for GUI application launch. If the user wants to open a specific file or directory in its associated application, use open_path instead; use start_process for terminal commands, build tools and shell pipelines. |
| `window_action` | changes state | `action*` ("focus" \| "minimize" \| "maximize" \| "restore" \| "move" \| "resize" \| "move_resize" \| "close"), `id` (string), `pid` (number), `app` (string), `title` (string), `x` (number), `y` (number), `width` (number), `height` (number), `backend` ("auto" \| "x11" \| "portal"), `device*` (string) | Use this when the user needs to focus, minimize, maximize, restore, move, resize or close one known top-level window. Select it by id/PID/app/title from list_windows. Use ui_action for controls inside the window; do not use pointer coordinates for window management when this semantic tool can express the action. |
| `screenshot_region` | read-only | `x` (number), `y` (number), `width` (number), `height` (number), `window_id` (string), `pid` (number), `app` (string), `title` (string), `monitor` (string), `monitor_index` (number), `padding` (number), `device*` (string) | Use this when static pixels are required for visual verification but only one window, monitor or rectangle matters. Prefer it over take_screenshot for a full desktop and over record_screen when motion/timing is not required; use ui_snapshot/browser_snapshot first when semantic structure is sufficient. |
| `open_path` | local state | `path*` (string), `device*` (string) | Use this when the user needs to open a permitted file or directory in its normal desktop application. Use launch_app when the task is to start an application itself without a particular file. Prefer open_path over shell-specific open/start/xdg-open commands because ReMCP applies filesystem confinement. |
| `reveal_path` | local state | `path*` (string), `device*` (string) | Use this when the user needs to reveal a permitted local file or directory in Finder/Explorer/file manager without opening the document itself. |
| `notification` | local state | `title` (string), `message*` (string), `timeout_seconds` (number), `device*` (string) | Use this when the user wants a visible native desktop notification on the paired computer. Do not use it as a substitute for replying in chat. |

## Native UI & input

Operate desktop applications through Accessibility/UI Automation first, with keyboard, pointer, drag, scroll, clipboard and cross-backend fallbacks when needed.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `ui_snapshot` | read-only | `scope` ("active" \| "desktop"), `pid` (number), `app` (string), `window_title` (string), `browser_dom` (boolean), `semantic_tree` (boolean), `max_nodes` (number), `max_depth` (number), `device*` (string) | Use this when the user needs to inspect native desktop Accessibility/UI Automation semantics for the active app before clicking or typing. Prefer scope=active; use scope=desktop only for cross-application discovery. For a Chromium web page with CDP available, prefer browser_snapshot for page DOM semantics. |
| `ui_find` | read-only | `id` (string), `label` (number), `pid` (number), `app` (string), `window_title` (string), `name` (string), `role` (string), `automation_id` (string), `refresh` (boolean), `limit` (number), `max_nodes` (number), `max_depth` (number), `device*` (string) | Use this when you need a small set of native UI elements by role/name/label/AutomationId after ui_snapshot. It requires an element criterion; use ui_snapshot, not an empty ui_find, to enumerate UI. Prefer returned id or compact label for subsequent ui_action; refresh only when the UI has changed. |
| `ui_action` | changes state, open-world | `action*` ("click" \| "invoke" \| "focus" \| "set_value" \| "select" \| "toggle" \| "expand" \| "collapse" \| "scroll_into_view" \| "set_range_value" \| "add_to_selection" \| "rem…), `id` (string), `label` (number), `pid` (number), `app` (string), `window_title` (string), `name` (string), `role` (string), `automation_id` (string), `value` (value), `device*` (string) | Use this when the user needs to invoke, focus, edit, select, toggle, expand/collapse, range-edit or scroll a native accessible UI element. Prefer this over pointer clicks because semantic actions survive layout changes; fall back to pointer only when accessibility cannot perform the requested action. |
| `type_text` | changes state, open-world | `text*` (string), `method` ("auto" \| "accessibility" \| "clipboard" \| "keys"), `clear` (boolean), `caret_position` ("start" \| "idle" \| "end"), `press_enter` (boolean), `delay_ms` (number), `id` (string), `label` (number), `pid` (number), `app` (string), `window_title` (string), `name` (string), `role` (string), `automation_id` (string), `window_id` (string), `title` (string), `backend` ("auto" \| "x11" \| "portal"), `device*` (string) | Use this when the task calls for reliable Unicode text entry into a native semantic target or focused native control. Prefer method=auto so Accessibility is used when the target supports verified semantic editing. For Chromium page DOM, use browser_action instead of forcing desktop accessibility text input; auto fails safely when a semantic element rejects the write rather than claiming an unverified paste. Use method=keys only for deliberate physical key-by-key input, and keyboard for shortcuts/navigation/control keys. |
| `keyboard` | changes state, open-world | `shortcut` (string), `key` (string), `keys` (array<string>), `window_id` (string), `pid` (number), `app` (string), `title` (string), `backend` ("auto" \| "x11" \| "portal"), `device*` (string) | Use this when the task calls for shortcuts and individual key presses such as Ctrl/Cmd+C, Tab, Enter or Escape. Do not use it to enter normal prose when type_text is available. On Wayland auto uses an already-authorized portal; backend=portal explicitly requests consent. |
| `pointer` | changes state, open-world | `action*` ("move" \| "click" \| "double_click" \| "right_click" \| "down" \| "up"), `x` (number), `y` (number), `button` ("left" \| "right" \| "middle"), `backend` ("auto" \| "x11" \| "portal"), `device*` (string) | Use this when an action truly requires screen coordinates or a semantic UI/browser action is unavailable. Prefer ui_action for native controls and browser_action for web controls; pointer is the coordinate fallback for move/click/button-down/button-up. |
| `drag_drop` | changes state, open-world | `from_x` (number), `from_y` (number), `to_x` (number), `to_y` (number), `from_id` (string), `to_id` (string), `button` ("left" \| "right" \| "middle"), `hold_ms` (number), `duration_ms` (number), `backend` ("auto" \| "x11" \| "portal"), `device*` (string) | Use this when the task calls for a real drag gesture between two UI element ids or coordinates. Prefer element ids from ui_find/ui_snapshot because their current bounds are resolved at execution time; use coordinates only when no semantic endpoints exist. On GNOME Wayland, real drag input requires the consent-backed Remote Desktop portal because XTEST/xdotool drag delivery is not reliable. |
| `scroll` | local state | `delta_x` (number), `delta_y` (number), `delta` (number), `direction` ("up" \| "down" \| "left" \| "right"), `wheel_times` (number), `x` (number), `y` (number), `id` (string), `label` (number), `pid` (number), `app` (string), `window_title` (string), `name` (string), `role` (string), `automation_id` (string), `backend` ("auto" \| "x11" \| "portal"), `device*` (string) | Use this when the task calls for native wheel scrolling of a desktop region or semantic UI target. Prefer browser_action scroll_into_view for a known web element and ui_action scroll_into_view for a known accessible desktop element. |
| `wait_for_ui` | read-only | `state` ("present" \| "absent" \| "changed"), `condition` ("text_exists" \| "active_window" \| "element_exists" \| "element_enabled" \| "focused_element"), `text` (string), `browser_dom` (boolean), `id` (string), `label` (number), `pid` (number), `app` (string), `window_title` (string), `name` (string), `role` (string), `automation_id` (string), `timeout_ms` (number), `poll_ms` (number), `device*` (string) | Use this when a desktop action must be followed by proof of an explicit native UI state or condition instead of sleeping or polling screenshots. Presence/absence waits need a semantic target; use state=changed for a broad tree change and set a bounded timeout. |
| `clipboard` | changes state | `action*` ("read" \| "write" \| "clear"), `text` (string), `device*` (string) | Use this when the task calls for explicit text clipboard read/write/clear operations. Do not use clipboard as a typing workaround directly; type_text already uses safe clipboard paste with restoration when needed. |
| `computer_action` | changes state, open-world | `action*` ("click" \| "invoke" \| "focus" \| "set_value" \| "select" \| "toggle" \| "expand" \| "collapse" \| "scroll_into_view" \| "set_range_value" \| "add_to_selection" \| "rem…), `target` ("auto" \| "ui" \| "ui_element" \| "browser" \| "ocr" \| "coordinates"), `id` (string), `label` (number), `pid` (number), `app` (string), `window_title` (string), `name` (string), `role` (string), `automation_id` (string), `title` (string), `text` (string), `value` (string), `x` (number), `y` (number), `selector` (string), `browser_text` (string), `ocr_text` (string), `ocr_region` (array<number>), `ocr_language` (string), `ocr_psm` (number), `ocr_timeout_ms` (number), `endpoint` (string), `target_id` (string), `url_contains` (string), `option` (string), `targets` (array<object>), `replace_selection` (boolean), `edits` (array<object>), `backend` ("auto" \| "x11" \| "portal"), `device*` (string) | Use this when one semantic click/edit/focus/selection action must work across an uncertain target backend and may need Accessibility → browser DOM/CDP → OCR → coordinate fallback, or when multi_select/multi_edit avoids many identical native UI calls. Do not use it when the backend or operation is already known: use ui_action for native controls, browser_action for page DOM, type_text for prose, keyboard for shortcuts, pointer for raw pointer gestures, drag_drop or scroll for those gestures, window_action for windows, and the dedicated clipboard/launch/open/reveal/notification tools for those jobs. |

## Browser DOM & CDP

Inspect and automate debuggable Chromium tabs semantically through loopback Chrome DevTools Protocol before falling back to desktop coordinates.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `browser_tabs` | read-only | `endpoint` (string), `target_id` (string), `title` (string), `url_contains` (string), `timeout_ms` (number), `device*` (string) | Use this when starting CDP browser automation and the target page or tab is not already unambiguous. It lists debuggable Chromium pages from a loopback-only endpoint; use the returned target_id/title/url to scope later browser tools. |
| `browser_navigate` | open-world | `endpoint` (string), `target_id` (string), `title` (string), `url_contains` (string), `timeout_ms` (number), `action` ("url" \| "new_tab" \| "back" \| "forward" \| "reload"), `url` (string), `wait` (boolean), `ignore_cache` (boolean), `device*` (string) | Use this when the user needs to open a URL in a new debuggable tab or navigate a known Chromium page by URL, back, forward or reload. Prefer it over typing into the address bar; new_tab also bootstraps browser automation when no page target exists yet. |
| `browser_snapshot` | read-only | `endpoint` (string), `target_id` (string), `title` (string), `url_contains` (string), `timeout_ms` (number), `max_nodes` (number), `include_screenshot` (boolean), `selector` (string), `device*` (string) | Use this when the user needs to inspect a web page structurally through CDP accessibility. Prefer it over desktop ui_snapshot for page content; add include_screenshot=true only when final pixel/layout verification is needed. A selector may be temporarily centered for capture, but the original page scroll position is restored before the tool returns. |
| `browser_find` | read-only | `endpoint` (string), `target_id` (string), `title` (string), `url_contains` (string), `timeout_ms` (number), `selector` (string), `text` (string), `role` (string), `limit` (number), `device*` (string) | Use this when the user needs to locate visible web elements by CSS selector, text or ARIA role and obtain reusable selectors/bounds. It requires an element criterion; use browser_snapshot, not an empty browser_find, to enumerate page structure. Prefer this before browser_action rather than guessing selectors or coordinates. |
| `browser_action` | changes state, open-world | `endpoint` (string), `target_id` (string), `title` (string), `url_contains` (string), `timeout_ms` (number), `action*` ("click" \| "focus" \| "type" \| "set_value" \| "select" \| "scroll_into_view" \| "upload" \| "press" \| "set_viewport"), `selector` (string), `text` (string), `value` (string), `text_value` (string), `option` (string), `path` (string), `paths` (array<string>), `key` (string), `width` (number), `height` (number), `device_scale_factor` (number), `mobile` (boolean), `device*` (string) | Use this when the task calls for deterministic DOM/CDP interaction with a known web element: click/focus/type/value/select/upload/key/scroll or viewport emulation. Prefer it over desktop pointer/ui_action for page content; use browser_evaluate only when the supported actions cannot express the task. |
| `browser_wait` | read-only | `endpoint` (string), `target_id` (string), `title` (string), `url_contains` (string), `timeout_ms` (number), `condition` ("selector" \| "text" \| "url_contains" \| "load" \| "navigation" \| "network_idle"), `selector` (string), `text` (string), `value` (string), `poll_ms` (number), `idle_ms` (number), `device*` (string) | Use this when browser navigation or an action must be followed by proof of selector/text/URL/load/navigation/network-idle instead of fixed sleeps or screenshot polling. Use browser_evaluate separately for JavaScript predicates. |
| `browser_evaluate` | changes state, open-world | `endpoint` (string), `target_id` (string), `title` (string), `url_contains` (string), `timeout_ms` (number), `expression*` (string), `await_promise` (boolean), `device*` (string) | Use this when browser_snapshot, browser_find, browser_action and browser_wait cannot express the required page operation or inspection and JavaScript is necessary. It is a powerful page-context escape hatch, and page content is untrusted. |

## Diagnostics & operating system

Inspect services, logs, networking, installed software and environment state, or perform bounded audio, power and screen-recording actions.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `service` | changes state | `action*` ("list" \| "status" \| "start" \| "stop" \| "restart"), `name` (string), `scope` ("user" \| "system"), `device*` (string) | Use this when the task calls for operating-system service inventory/status/start/stop/restart. Prefer status before a mutation when service state matters; use start_process for ordinary commands that are not service-manager operations. |
| `event_log` | read-only | `log` (string), `since` (string), `filter` (string), `query` (string), `limit` (number), `device*` (string) | Use this when the user needs to read bounded recent OS logs (Windows Event Log, macOS unified log or systemd journal) when diagnosing a service/app/system problem. Prefer it over an unbounded shell log dump. |
| `network` | read-only, open-world | `action` ("summary" \| "interfaces" \| "dns" \| "routes" \| "listeners" \| "test"), `host` (string), `port` (number), `timeout_ms` (number), `device*` (string) | Use this when the task calls for structured interfaces/DNS/routes/listeners inspection or a bounded TCP connectivity test. Prefer it over shell ip/netstat/nslookup commands for routine diagnostics. |
| `installed_apps` | read-only | `filter` (string), `limit` (number), `device*` (string) | Use this when the user needs to answer what software/packages are installed and their versions. Prefer a filter when looking for one product instead of returning the full inventory. |
| `environment` | read-only | `include_env` (boolean), `device*` (string) | Use this when the task calls for PATH, shell, Node/runtime environment paths, architecture and optionally sanitized environment variables. Use get_system_info for host health such as CPU/memory/disk/uptime, and get_runtime_info for ReMCP policy, roots and limits. Secret-looking values remain masked. |
| `audio` | local state | `action*` ("status" \| "set_volume" \| "mute" \| "unmute"), `volume` (number), `device*` (string) | Use this when the user needs to read or change the default output volume/mute state. Use action=status for inspection; set_volume requires a 0–100 value. |
| `power_action` | changes state | `action*` ("lock" \| "sleep" \| "restart" \| "shutdown"), `delay_seconds` (number), `device*` (string) | Use this when the user explicitly asks to lock, sleep, restart or shut down the computer. Prefer it over invoking those system actions through start_process; it is intentionally guarded by device command policy because restart/shutdown can interrupt work. |
| `record_screen` | changes state | `duration_seconds` (number), `fps` (number), `destination` (string), `device*` (string) | Use this when the task calls for a short bounded screen recording when motion/timing is necessary to diagnose a UI issue. Prefer screenshot_region for static visual verification because it is cheaper and easier to inspect. |

## Structured documents

Read common document formats and edit XLSX, DOCX or PDF structure directly without driving Office applications.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `read_document` | read-only | `path*` (string), `sheet` (string), `max_cells` (number), `device*` (string) | Use this when the task calls for structured document content, especially PDF, DOCX and XLSX, and for CSV/JSON/XML when document-style parsing is useful. Use read_file for ordinary source code, logs, configuration, Markdown, or other plain line-oriented text. Prefer read_document over shell extraction or launching Office when the goal is document content; PDF reading uses the built-in parser first and may fall back to local pdftotext when embedded fonts prevent direct decoding. |
| `edit_spreadsheet` | changes state | `path*` (string), `create` (boolean), `output` (string), `sheet` (string), `edits*` (array<object>), `device*` (string) | Use this when the user needs to create a new XLSX workbook or directly edit XLSX cells/ranges/formulas without launching Excel. Set create=true only for a new path; it fails rather than overwriting an existing workbook. Prefer explicit cells/ranges and write to output when preserving an existing original matters. |
| `edit_document` | changes state | `path*` (string), `create` (boolean), `output` (string), `operations*` (array<object>), `device*` (string) | Use this when the user needs to create a new DOCX document or make structured DOCX paragraph edits without launching Word. Set create=true only for a new path; it fails rather than overwriting an existing document. It is best for replace/insert/delete paragraph operations; use native UI automation only when document formatting/layout interaction is required. |
| `pdf_action` | changes state | `action*` ("merge" \| "split" \| "extract_pages" \| "annotations" \| "info"), `path` (string), `paths` (array<string>), `output` (string), `output_dir` (string), `pattern` (string), `pages` (string), `device*` (string) | Use this when the task calls for PDF metadata/annotations inspection or structural merge/split/page extraction. For reading PDF text use read_document; supply an output/output_dir for write actions when preserving originals matters. |

## Terminal & processes

Run commands, watch long-running output, answer prompts, and inspect or stop sessions and processes.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `start_process` | changes state, open-world | `command*` (string), `timeout_ms` (number), `device*` (string) | Use this when the user asks to run a shell command, build, test suite, script, or terminal server that is not already covered by a narrower semantic tool. Prefer launch_app for GUI applications, service for service-manager operations, power_action for lock/sleep/restart/shutdown, network for routine network inspection/tests, event_log for OS logs, installed_apps for software inventory, environment for environment facts, and open_path/reveal_path for opening or revealing files. |
| `read_process_output` | read-only | `pid*` (number), `offset` (number), `length` (number), `timeout_ms` (number), `device*` (string) | Use this when a process started earlier has more output to show and you do not want to wait for a pattern. |
| `interact_with_process` | changes state, open-world | `pid*` (number), `input*` (string), `timeout_ms` (number), `device*` (string) | Use this when a running process is waiting for input, such as a prompt or a confirmation. |
| `wait_for_process_output` | read-only | `pid*` (number), `pattern*` (string), `timeout_ms` (number), `device*` (string) | Use this when you need to wait until a running command prints something specific, such as a readiness line or a failing test name. |
| `force_terminate` | changes state | `pid*` (number), `device*` (string) | Use this when a process session created by start_process must stop, such as a runaway build or server left behind. For an arbitrary operating-system PID that is not a ReMCP terminal session, use kill_process instead. |
| `list_sessions` | read-only | `device*` (string) | Use this when you need to know which ReMCP terminal sessions created by start_process are still running. Use list_processes for the computer-wide operating-system process list. |
| `list_processes` | read-only | `limit` (number), `device*` (string) | Use this when the user wants the computer-wide operating-system process list. Use list_sessions when the question is only about commands started through ReMCP start_process. |
| `kill_process` | changes state | `pid*` (number), `device*` (string) | Use this when the user wants to stop an arbitrary operating-system process by PID. If the PID belongs to a ReMCP terminal session created by start_process, prefer force_terminate so the session state and process tree are handled consistently. |

## System & runtime

Inspect the machine and ReMCP runtime, then change only the small set of model-safe runtime preferences.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `get_system_info` | read-only | `device*` (string) | Use this when the user asks about machine health and host facts such as operating system, CPU load, memory, free disk space, or uptime. Use environment for PATH/shell/runtime environment details, and get_runtime_info for ReMCP-specific version, policy, and limits. |
| `get_runtime_info` | read-only | `device*` (string) | Use this when you need one computer's local ReMCP runtime version, guardrails, allowed roots, or configured limits. Use get_system_info for host OS/CPU/memory/disk facts, and get_configuration for deployment/account-level ReMCP server configuration. |
| `get_runtime_stats` | read-only | `device*` (string) | Use this when you need counters from the current local runtime session on one machine: tool calls, failures, blocks, and active sessions. Use get_usage_statistics for account-level historical usage aggregated by device/tool over days. |
| `set_config_value` | local state | `key*` (string), `value*` (value), `device*` (string) | Use this when the person wants to change a local runtime preference on one computer: telemetry opt-out, read/buffer line limits, or result size. Do not use this for account-level computer administration; device_action owns ping, rename, enable/disable and removal. Access roots, blocked commands, shell/guardrails and the write limit stay local-only. |


## Contract notes

- Every public tool has a human-readable title/description, explicit input schema, output schema for structured results, and MCP safety annotations in the production catalog.
- Structured tool calls return `structuredContent` that matches the advertised output schema together with normal MCP content where useful to the model.
- Browser/CDP tools are capability-aware and can be enabled or disabled as the local runtime discovers or loses a usable loopback browser endpoint.
- `computer_snapshot` is the broad first-look tool for an unfamiliar desktop; once the target is known, prefer the narrowest dedicated tool for the action and verify with semantic state before using pixels.
- The website renders this same catalog at [remcp.site/docs#tools](https://remcp.site/docs#tools).
