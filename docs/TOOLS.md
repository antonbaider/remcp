# ReMCP tool reference

> Generated by `scripts/generate-web-tool-docs.mjs` from the production MCP catalog. Do not edit this file by hand.

The production catalog advertises **15 hosted tools** when custom widgets are enabled, but only **10 are model-visible**: **8 device-facing façade tools** plus **2 account/fleet tools**. The remaining **5 tools are app-only UI helpers** for preview hydration, file-editor save, and terminal refresh. Those compact definitions route to **83 granular runtime operations**. Cached clients can still call legacy granular names, so the compatibility call surface contains **105 unique names** without forcing a model to choose among all of them. Setting `REMCP_CUSTOM_WIDGETS_ENABLED=false` exposes the same 10-tool model-facing surface without app-only helpers or preview metadata. Hosted discovery stays release-stable; each connected computer advertises its exact live runtime subset through `list_devices`, and the server checks the selected operation against that device immediately before dispatch.

The app-only helpers never add model-selected action paths: render tools rehydrate already-returned file/image/terminal previews, `save_file_preview` writes the exact editor contents through the underlying `write_file` schema, and `refresh_terminal_preview` reads more output through the underlying `read_process_output` schema.

For visible interaction, use **Accessibility/UI Automation → browser DOM/CDP → OCR → coordinates**. Prefer structured document tools over automating Office applications when the task is about file content rather than interactive formatting.

Required parameters are marked with `*`. Tool descriptions, parameter names/types, and safety hints below come from the same catalog that the server advertises.

## Account & fleet

List paired computers and use one account lane for profile, usage, hosted configuration, pairing, ping, rename, enable/disable, or removal.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `list_devices` | read-only | None | Use this when the target computer is not already unambiguous. Call it first to list computers paired to the signed-in ReMCP account and see which ones are online. |
| `manage_account` | changes state | `operation*` ("profile" \| "recent_calls" \| "usage" \| "configuration" \| "pair" \| "ping" \| "rename" \| "enable" \| "disable" \| "remove"), `device` (string), `name` (string), `limit` (number ≥1 ≤100), `days` (number ≥1 ≤90), `tool` (string) | Use this when the task concerns the signed-in ReMCP account or paired-computer membership: inspect profile, recent calls, usage or hosted configuration; create a pairing; or ping, rename, enable, disable, or permanently remove one pairing. Use list_devices first when the target computer is ambiguous. Local runtime preferences belong to manage_system. |

Operations: `profile`, `recent_calls`, `usage`, `configuration`, `pair`, `ping`, `rename`, `enable`, `disable`, `remove`

## App-only UI helpers

MCP Apps-only helpers rehydrate previews, save the file editor, or refresh terminal output without exposing extra choices to the model.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `render_file_preview` | read-only | `preview_id*` (string) | Use this when the file preview app must rehydrate already-returned read_file content after late or missing result metadata. This presentation-only helper reads only the bounded preview cache and never re-reads or writes the computer. Use save_file_preview to persist editor changes and read_file for a fresh file read. |
| `render_image_preview` | read-only | `preview_id*` (string) | Use this when the image preview app must rehydrate already-returned read_image or screenshot bytes after late or missing result metadata. This presentation-only helper reads only the bounded preview cache and never captures or reads the computer again. Use view_image when fresh image or screenshot pixels are required. |
| `render_terminal_preview` | read-only | `preview_id*` (string) | Use this when the terminal preview app must rehydrate already-returned process output after late or missing result metadata. This presentation-only helper reads only the bounded preview cache and never contacts or reruns the process. Use refresh_terminal_preview for fresh buffered output and run_terminal for process operations. |
| `save_file_preview` | changes state | `path*` (string), `content*` (string), `mode` ("rewrite" \| "append"), `device*` (string) | Use this when the ReMCP file preview app needs to save the user-edited contents back to that same paired computer. It exposes only the underlying write_file schema; general model file work belongs to manage_files. |
| `refresh_terminal_preview` | read-only | `pid*` (number), `offset` (number), `length` (number), `timeout_ms` (number), `device*` (string) | Use this when the ReMCP terminal preview app needs a fresh read of buffered output from the same existing process. With no offset it reads new output and can advance the session cursor; offset/timeout control the bounded read. Use render_terminal_preview only to rehydrate already-returned preview data, and run_terminal to start, interact with, or terminate a process. |

## Files

Read one known text file directly, or use the grouped file lane for batch reads, edits, metadata, search, archives, moves, copies, permissions and deletion.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `read_file` | read-only | `path*` (string), `offset` (number), `length` (number), `device*` (string) | Use this when the task calls for plain text files on a paired computer: source code, configuration, Markdown, logs, scripts, or other line-oriented text. Use read_document instead for PDF, DOCX, XLSX and other structured document content. read_file keeps legacy PDF/DOCX extraction for compatibility but should not be the model’s first choice for those formats. |
| `manage_files` | changes state | `device*` (string), `operation*` ("read_files" \| "read_multiple_files" \| "read_binary" \| "write_binary" \| "hash_file" \| "list_directory" \| "get_file_info" \| "write_file" \| "write_files" \| "ap…), `parameters` (object) | Use this when the task is about files, directories, archives, or filesystem search on one paired computer. Choose exactly one operation; parameters must contain only that operation’s arguments and are validated against its closed schema. Write, edit, move, permission, and delete operations can change or remove local data or fail on OS permissions, while read and search operations leave files unchanged. Use read_file instead for one known text file so the native file preview remains available. |

Operations: `read_files`, `read_multiple_files`, `read_binary`, `write_binary`, `hash_file`, `list_directory`, `get_file_info`, `write_file`, `write_files`, `apply_patch`, `set_permissions`, `edit_block`, `replace_lines`, `replace_in_files`, `diff_files`, `move_to_trash`, `create_directory`, `move_file`, `copy_file`, `copy_paths`, `move_paths`, `delete_path`, `delete_paths`, `create_archive`, `extract_archive`, `start_search`, `get_more_search_results`, `stop_search`, `list_searches`

## Terminal

Run commands and manage ReMCP terminal sessions through one grouped lane while preserving each concrete process operation.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `run_terminal` | changes state, open-world | `device*` (string), `operation*` ("start_process" \| "read_process_output" \| "interact_with_process" \| "wait_for_process_output" \| "force_terminate" \| "list_sessions"), `parameters` (object) | Use this when the task needs a shell command or an existing ReMCP terminal session: start, read, wait, interact, stop, or list sessions. Choose exactly one operation; parameters are validated against that operation’s closed schema. Starting or interacting with commands can change local or external state, and force termination can lose unsaved process work; reading, waiting, and listing do not mutate the session. Use manage_system for OS-wide process inventory or killing an arbitrary PID. |

Operations: `start_process`, `read_process_output`, `interact_with_process`, `wait_for_process_output`, `force_terminate`, `list_sessions`

## Desktop & vision

Inspect semantic desktop state and control native applications through one computer lane; request pixels through the separate image lane only when visual evidence is required.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `control_computer` | changes state, open-world | `device*` (string), `operation*` ("computer_snapshot" \| "list_windows" \| "ui_snapshot" \| "ui_find" \| "wait_for_ui" \| "display_inventory" \| "computer_action" \| "window_action" \| "launch_app" \|…), `parameters` (object) | Use this when the task needs semantic inspection or control of native desktop applications: snapshots, windows, accessibility/UI elements, waits, displays, app launch, text/input, clipboard, open/reveal, notifications, or short screen recording. Semantic snapshot/find/wait operations observe state, while clicks, typing, window close, app launch, clipboard writes, and recording can change application or local state. Prefer semantic inspection/actions before raw coordinates; use view_image only when pixels are required and control_browser for page DOM/CDP actions. |

Operations: `computer_snapshot`, `list_windows`, `ui_snapshot`, `ui_find`, `wait_for_ui`, `display_inventory`, `computer_action`, `window_action`, `launch_app`, `ui_action`, `type_text`, `keyboard`, `pointer`, `drag_drop`, `scroll`, `clipboard`, `open_path`, `reveal_path`, `notification`, `record_screen`
| `view_image` | local state | `device*` (string), `operation*` ("read_image" \| "take_screenshot" \| "screenshot_region"), `parameters` (object) | Use this when the result must be pixels from a paired computer: read an existing image, capture the desktop, or capture one window, monitor, or region. These operations return pixels without interacting with the visible UI; use control_computer or control_browser instead when semantic structure or an action is enough. |

Operations: `read_image`, `take_screenshot`, `screenshot_region`

## Browser DOM & CDP

Inspect and automate debuggable Chromium tabs through semantic DOM/CDP operations before falling back to desktop coordinates.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `control_browser` | changes state, open-world | `device*` (string), `operation*` ("browser_tabs" \| "browser_navigate" \| "browser_snapshot" \| "browser_find" \| "browser_action" \| "browser_wait" \| "browser_evaluate"), `parameters` (object) | Use this when the target is a debuggable Chromium page and the task needs tabs, navigation, DOM/accessibility inspection, element search/actions, waits, or explicit JavaScript evaluation. Snapshot, find, and wait operations observe page state; navigation, clicks, typing, uploads, and JavaScript evaluation can change page or remote-site state. Choose one operation and pass only its validated parameters. Use control_computer for browser chrome or other native desktop/non-CDP applications. |

Operations: `browser_tabs`, `browser_navigate`, `browser_snapshot`, `browser_find`, `browser_action`, `browser_wait`, `browser_evaluate`

## System & diagnostics

Inspect and manage OS/runtime state, processes, services, logs, networking, installed apps, environment, audio and power operations.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `manage_system` | changes state, open-world | `device*` (string), `operation*` ("get_system_info" \| "list_processes" \| "kill_process" \| "get_runtime_info" \| "get_runtime_stats" \| "set_config_value" \| "service" \| "event_log" \| "network" \|…), `parameters` (object) | Use this when the task concerns operating-system or ReMCP runtime state: system/runtime/process facts, runtime preferences, services, event logs, networking, installed apps, environment, audio, or explicit power actions. Fact, log, and inventory operations only observe state; process termination, runtime preference changes, service/audio changes, restart, and shutdown can interrupt work or change the machine. Choose one operation and pass only its validated parameters. Use run_terminal for general shell commands and ReMCP terminal sessions. |

Operations: `get_system_info`, `list_processes`, `kill_process`, `get_runtime_info`, `get_runtime_stats`, `set_config_value`, `service`, `event_log`, `network`, `installed_apps`, `environment`, `audio`, `power_action`

## Structured documents

Read common document formats and edit XLSX, DOCX or PDF structure directly without driving Office applications.

| Tool | Safety | Parameters | When to use |
| --- | --- | --- | --- |
| `manage_documents` | changes state | `device*` (string), `operation*` ("read_document" \| "edit_spreadsheet" \| "edit_document" \| "pdf_action"), `parameters` (object) | Use this when the task is structured document content: read PDF/DOCX/XLSX, edit DOCX/XLSX directly, or merge, split, extract, or inspect PDFs. DOCX/XLSX edits can replace the input when no separate output is supplied, and PDF write operations create or replace their requested output; read_document itself is read-only. Choose one operation and pass only its validated parameters. Use read_file for plain line-oriented text and control_computer only for interactive Office UI. |

Operations: `read_document`, `edit_spreadsheet`, `edit_document`, `pdf_action`


## Contract notes

- Every public tool has a human-readable title/description, explicit input schema, output schema for structured results, and MCP safety annotations in the production catalog.
- Structured tool calls return `structuredContent` that matches the advertised output schema together with normal MCP content where useful to the model.
- Browser/CDP tools are capability-aware and can be enabled or disabled as the local runtime discovers or loses a usable loopback browser endpoint.
- `computer_snapshot` is the broad first-look tool for an unfamiliar desktop; once the target is known, prefer the narrowest dedicated tool for the action and verify with semantic state before using pixels.
- The website renders this same catalog at [remcp.site/docs#tools](https://remcp.site/docs#tools).
